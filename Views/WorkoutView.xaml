<UserControl x:Class="GymTrackerApp.Views.WorkoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GymTrackerApp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:AddCustomExercisePickerItem}">
            <Button Content="➕ Добавить своё упражнение"
                    Padding="10,6"
                    HorizontalContentAlignment="Left"
                    Focusable="False"
                    Command="{Binding DataContext.AddCustomExerciseCommand,
                                      RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ExerciseViewModel}">
            <StackPanel>
                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                <TextBlock Text="{Binding MuscleGroup}" FontSize="12" Foreground="Gray"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Content="Удалить"
                Padding="10,5"
                Command="{Binding DeleteCommand}"
                Margin="20 20 0 0"
                HorizontalAlignment="Left">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Grid.Row="0"
                Content="Закончить"
                HorizontalAlignment="Right"
                Margin="0 20 20 0"
                Padding="10,5"
                Command="{Binding FinishCommand}">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Grid Grid.Row="1" Margin="0 20 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- Время -->
            <StackPanel Grid.Column="0"
                        Orientation="Vertical"
                        HorizontalAlignment="Center">
                <TextBlock Text="Время"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentDuration, StringFormat={}{0:hh\\:mm\\:ss}}"
                           FontSize="12"
                           Margin="0,4,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Объём -->
            <StackPanel Grid.Column="1"
                        Orientation="Vertical"
                        HorizontalAlignment="Center">
                <TextBlock Text="Объём"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TotalWeight, StringFormat={}{0:N0}}"
                           FontSize="12"
                           Margin="0,4,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Сеты -->
            <StackPanel Grid.Column="2"
                        Orientation="Vertical"
                        HorizontalAlignment="Center">
                <TextBlock Text="Сеты"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TotalSets}"
                           FontSize="12"
                           Margin="0,4,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>


        <TextBlock Grid.Row="2"
                   Margin="20 10 20 0"
                   Text="{Binding Description}"
                   TextWrapping="Wrap"
                   Foreground="Gray"/>

        <Separator Grid.Row="3" Margin="15 15"/>

        <Grid Grid.Row="4">
            <ScrollViewer>
                <StackPanel>

                    <ItemsControl ItemsSource="{Binding WorkoutExercises}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="20 10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <ComboBox Grid.Column="0"
                                                      ItemsSource="{Binding DataContext.ExerciseDefinitions,
                                                          RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      SelectedItem="{Binding ExerciseDefinition, Mode=TwoWay}">
                                                <ComboBox.Style>
                                                    <Style TargetType="ComboBox">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                         Value="True">
                                                                <Setter Property="IsEnabled" Value="False"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ComboBox.Style>

                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                            <TextBlock Text="{Binding MuscleGroup}" FontSize="12" Foreground="Gray"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>

                                            <Border Grid.Column="1"
                                                    CornerRadius="6"
                                                    Margin="8 0 0 0"
                                                    VerticalAlignment="Center">
                                                <Button Command="{Binding DataContext.RemoveExerciseCommand,
                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                             Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>

                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border CornerRadius="6"
                                                                    Width="32"
                                                                    Height="32"
                                                                    Background="#f0f0f0"
                                                                    BorderBrush="#d0d0d0"
                                                                    BorderThickness="1">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                    <TextBlock FontSize="15" Text="✕" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                </Button>
                                            </Border>
                                        </Grid>

                                        <Grid Grid.Row="1" Margin="20 4">
                                            <Border Padding="6" Margin="0 0 25 0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Column="0" Text="Подх." VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="1" Text="KG" VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="2" Text="Повт." VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Grid>

                                    <Separator Margin="20 0 40 5"/>

                                    <ItemsControl ItemsSource="{Binding Sets}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="20 4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0"
                                                            Padding="6"
                                                            Background="#f0f0f0"
                                                            BorderBrush="#d0d0d0"
                                                            BorderThickness="1"
                                                            CornerRadius="6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0"
                                                                       Text="{Binding Order}"
                                                                       VerticalAlignment="Center"/>

                                                            <TextBox Grid.Column="1"
                                                                     Text="{Binding Weight, UpdateSourceTrigger=PropertyChanged}"
                                                                     Margin="0 0 10 0"
                                                                     IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                                                            <TextBox Grid.Column="2"
                                                                     Text="{Binding Reps, UpdateSourceTrigger=PropertyChanged}"
                                                                     Margin="0 0 10 0"
                                                                     IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                                        </Grid>
                                                    </Border>

                                                    <Border Grid.Column="1"
                                                            CornerRadius="6"
                                                            Margin="8 0 0 0"
                                                            VerticalAlignment="Center">
                                                        <Button Command="{Binding DataContext.RemoveSetCommand,
                                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                     Value="True">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>

                                                            <Button.Template>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border CornerRadius="6"
                                                                            Width="24"
                                                                            Height="24"
                                                                            Background="#f0f0f0"
                                                                            BorderBrush="#d0d0d0"
                                                                            BorderThickness="1">
                                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                                          VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Button.Template>

                                                            <TextBlock Text="✕" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                                        </Button>
                                                    </Border>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Button Content="Добавить сет"
                                            Margin="60 5 60 0"
                                            Height="20"
                                            Command="{Binding AddSetCommand}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                 Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Grid Margin="30 10 30 20">
                        <Button Content="Добавить упражнение"
                                Height="30"
                                Command="{Binding StartAddExerciseCommand}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAddingExercise}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <ComboBox ItemsSource="{Binding ExercisePickerItems}"
                                  SelectedItem="{Binding SelectedExercisePickerItem, Mode=TwoWay}"
                                  IsDropDownOpen="{Binding IsComboOpen, Mode=TwoWay}"
                                  IsTextSearchEnabled="True"
                                  IsEditable="True"
                                  StaysOpenOnEdit="True"
                                  TextSearch.TextPath="Name"
                                  Height="30">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAddingExercise}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
