<Window x:Class="GymTrackerApp.Views.AddExerciseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить упражнение"
        Width="360" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название" />
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,6,0,10" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="Группа мышц" Margin="0,0,0,6"/>
            <ComboBox x:Name="GroupBox" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Отмена" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Добавить" Width="90" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
